<nav id='second-navbar' class="navbar navbar-default" role="navigation">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#second-navbar-collapse">
      <span class="sr-only"><%= t('common.toggle_menu') %></span>
    </button>
  </div>

  <div class="collapse navbar-collapse" id="second-navbar-collapse">
    <ul class="nav navbar-nav">
      <% available_categories.each do |category| %>
        <li><%= link_to category.name, store_browse_path(id: category.id) %></li>
      <% end %>
    </ul>
  </div>
</nav>