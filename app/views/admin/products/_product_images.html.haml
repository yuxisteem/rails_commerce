= form_tag do
  .form-group
    = label_tag 'images'
    #admin-images-list.row
      = render product.images
      = image_upload_tag 'product', product.id, url: admin_product_images_path(product_id: product.id)
      %ul#images
